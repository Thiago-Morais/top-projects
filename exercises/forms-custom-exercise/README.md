_Generated by DeepSeak_
Here's the complete, descriptive requirements specification for the registration form. Every form element and attribute from the original example is covered—without ever naming an HTML attribute directly. The programmer will need to deduce the correct elements and properties from these functional descriptions.

* * *

# Specification: National Health Research Study & Wellness Workshop Registration Form

## **Overall Form Behavior**

* The form must send its data to the URL `https://example.com/api/submit-research-registration` using the HTTP POST method.

* The data encoding must support file uploads (multipart form data).

* The form must be identified internally with the name `researchRegistration`.

* Upon successful submission, the response must open in a new browsing context (new tab/window).

<!-- * Client-side validation must be bypassed when the form is submitted using its default submit button; the browser should not block incomplete fields. -->

* The browser’s autocomplete mechanism may suggest previously entered values for fields, unless explicitly overridden for a specific field.

* The form must accept text input encoded in UTF-8.

* A privacy-related hint must be added to the form that indicates no referrer information should be sent when navigating to the action URL.

## **Progress Indicator**

* A visual completion progress bar must be displayed before the first section of the form. It should initially show 0% progress, with an upper bound of 100%. This indicator is associated with the main form.

* * *

## **Section 1: Account Setup**

* This section must be visually grouped and labeled “Section 1: Account Setup”. It belongs to the main form.

1. **Username field**

    * A text input for a chosen username.

    * It must be filled out before the form can be submitted.

    * A hint text inside the field says “e.g., johndoe\_research”.

    * The browser should, if possible, offer to autofill a username.

    * When the page loads, the cursor must immediately be placed in this field.

    * The visible width of the field should accommodate approximately 30 characters.

    * The entered value must be at least 4 characters and no more than 24 characters long.

    * Only letters (a–z, A–Z), digits (0–9), and underscores are permitted.

    <!-- * The field must also submit the directionality of the text (left‑to‑right or right‑to‑left) under a name derived by appending `.dir` to the field’s name. -->

    <!-- * The on‑screen keyboard should be optimized for general text entry. -->

    <!-- * This field explicitly belongs to the main form, even if placed outside the `<form>` container (but here it is inside). -->

2. **Password field**

    * A password input where characters are masked.

    * Filling this field is mandatory.

    * Minimum length: 8 characters; maximum: 64 characters.

    * Must contain at least one digit, one lowercase letter, and one uppercase letter.

    * Placeholder text: “Min 8 chars, 1 uppercase, 1 number”.

    * The browser must suggest previously used passwords; it should treat it as a new password.

    * Visible width: approximately 30 characters.

    <!-- * Belongs to the main form. -->

3. **Email address field**

    * An input specifically for email addresses.

    * Must be completed before submission.

    * The user may enter multiple email addresses separated by commas.

    * Placeholder: “<you@example.com>”.

    * Browser autofill for email is allowed.

    * Width: about 35 characters.

    <!-- * On‑screen keyboard: email mode. -->

    <!-- * Belongs to the main form. -->

4. **Phone number field**

    * A telephone number input.

    * Placeholder: “+1 (555) 000‑0000”.

    * Valid characters include digits, spaces, hyphens, parentheses, and an optional leading plus sign. <!-- The total length must be between 7 and 20 characters. -->

    * Autofill: national telephone number.

    * Width: 25 characters.

    <!-- * Belongs to the main form. -->

5. **Professional Profile URL field**

    * A URL input.

    * Placeholder: “[https://www.linkedin.com/in/yourprofile](https://www.linkedin.com/in/yourprofile)”.

    * Autofill: URL.

    * Width: 40 characters.

    <!-- * Belongs to the main form. -->

* * *

## **Section 2: Personal Information**

* Visually grouped and labeled “Section 2: Personal Information”. Belongs to the main form.

1. **Title / Prefix field with predefined suggestions**

    * A text input that also provides a dropdown list of suggestions.

    * The list of suggestions contains: “Dr.”, “Mr.”, “Mrs.”, “Ms.”, “Mx.”, “Prof.”

    * Placeholder: “Select or type your title”.

    * Visible width: 20 characters.

    * Browser autofill: honorific prefix.

    <!-- * Belongs to the main form. -->

2. **Participant ID (read‑only)**

    * A text input pre‑filled with the value “PR‑2026‑784921”.

    * The user must not be able to modify this value.

    * Width: 25 characters.

    <!-- * Belongs to the main form. -->

3. **Name Badge Color**

    * A color picker.

    * Default initial color: `#336699`.

    <!-- * Belongs to the main form. -->

4. **Date of Birth**

    * A date picker.

    * Mandatory field.

    * Earliest allowed date: January 1, 1900.

    * Latest allowed date: December 31, 2026.

    * Step increment: 1 day.

    * Autofill: birthday.

    <!-- * Belongs to the main form. -->

5. **Preferred Contact Time**

    * A time picker.

    * Allowed range: 08:00 to 20:00.

    * Step increment: 15 minutes (900 seconds).

    * Default value: 10:00.

    <!-- * Belongs to the main form. -->

6. **Onboarding Session**

    * A combined date and time picker.

    * Mandatory.

    * Earliest allowed moment: July 1, 2026, 08:00.

    * Latest: December 31, 2026, 18:00.

    * Step increment: 30 minutes (1800 seconds).

    <!-- * Belongs to the main form. -->

7. **Eligibility Start Month**

    * A month‑year picker.

    * Range: from January 2026 to December 2027.

    <!-- * Belongs to the main form. -->

8. **Preferred Study Start Week**

    * A week‑year picker.

    * Range: week 27 of 2026 to week 52 of 2026.

    * Step: 1 week.

    <!-- * Belongs to the main form. -->

9. **Gender selection**

    * A group of four options where only one can be selected.

    * Options: “Female”, “Male”, “Non‑binary”, “Prefer not to say”.

    * The “Female” option is pre‑selected.

    * All options belong to the same form, using the name `gender`, and values `female`, `male`, `non_binary`, `prefer_not_to_say` respectively.

    <!-- * Each option is explicitly tied to the main form. -->

* * *

## **Section 3: Health Metrics (currently locked)**

* This entire section must be visually grouped and labeled “Section 3: Health Metrics (currently locked - complete previous sections)”.

* All interactive elements inside this section must be in a disabled, non‑interactive state.

1. **Weight (kg)**

    * A numeric field.

    * Limits: minimum 30, maximum 300.

    * Step: 0.1.

    * Default value: 70.

    * Placeholder: “Enter weight in kilograms”.

    <!-- * On‑screen keyboard: decimal input. -->

    * Disabled.

    <!-- * Belongs to the main form. -->

2. **Height (cm)**

    * A numeric field.

    * Limits: 100 to 250, step 0.1.

    * Default: 170.

    * Placeholder: “Enter height in centimeters”.

    * Decimal keyboard.

    * Disabled.

    <!-- * Belongs to the main form. -->

3. **Calculated BMI (output only)**

    * A read‑only display element that is linked to the weight and height fields for calculation purposes.

    * It must show “--” initially.

    <!-- * The result must be associated with the form under the name `bmiResult`. -->

    <!-- * Belongs to the main form. -->

4. **Current Pain Level**

    * A slider (range) control.

    * Range: 0 to 10, step 1.

    * Default value: 3.

    * Tick mark suggestions at every integer from 0 through 10 must be provided.

    <!-- * Disabled. -->

    <!-- * Belongs to the main form. -->

5. **Blood Oxygen Saturation Gauge**

    * A scalar gauge representing a percentage.

    * Value: 97%.

    * Full range: 70 to 100.

    * The gauge must indicate that values below 90 are low, values above 96 are high, and 98 is the optimum.

    * The numeric value “97%” must be visible.

    <!-- * Belongs to the main form. -->

6. **Pre‑existing Medical Conditions**

    * A list box that allows multiple selections.

    * Must have a visible height of 6 options (scrollable).

    * At least one selection is required.

    <!-- * Autocomplete is turned off. -->

    <!-- * Disabled. -->
    <!-- * Belongs to the main form. -->

    The options are organized into groups:

    * **Cardiovascular**: Hypertension, Arrhythmia, Congestive Heart Failure.

    * **Respiratory**: Asthma, COPD, Sleep Apnea.

    * **Metabolic** (this entire group is disabled and its options cannot be selected): Type 1 Diabetes, Type 2 Diabetes, Thyroid Disorder.

    * **Other**: “None of the above” (pre‑selected), and “Other (specify in notes)” (which is individually disabled).

    The values for the conditions must be single lowercase words or snake\_case as appropriate: `hypertension`, `arrhythmia`, `chf`, `asthma`, `copd`, `sleep_apnea`, `diabetes_type1`, `diabetes_type2`, `thyroid`, `none`, `other`.

7. **Blood Type**

    * A dropdown (single selection) list.

    <!-- * Disabled. -->

    <!-- * Belongs to the main form. -->

    * The first option is a placeholder that reads “-- Select Blood Type --” and is disabled and selected by default.

    * Subsequent options: A+, A‑, B+, B‑, AB+, AB‑, O+, O‑.

    * The last option has the visible text “Unknown / Not Tested” but its underlying value is `Unknown`.

    * Values should match exactly: `A+`, `A-`, etc.

* * *

## **Section 4: Document Uploads**

* Visually grouped and labeled “Section 4: Document Uploads”. Belongs to the main form.

1. **Government‑Issued Photo ID upload**

    * A file chooser.

    * Mandatory.

    * Accepted file types: PNG, JPEG, WebP, and PDF (specified as MIME types `image/png`, `image/jpeg`, `image/webp`, `application/pdf`).

    * The file picker should, if the device supports it, prefer the front‑facing camera for immediate capture.

    <!-- * Belongs to the main form. -->

2. **Medical Records upload**

    * A file chooser that allows selecting multiple files at once.

    * Accepted file extensions: `.pdf`, `.doc`, `.docx`, `.jpg`, `.png`, `.dcm`.

    * The file picker should prefer the rear‑facing camera for document scanning.

    <!-- * Belongs to the main form. -->

3. **Hidden security token**

    * A hidden field named `formToken` with the value `a8f3c9e2b1d4f6a7c8e9d0b1a2c3d4e5`.

    <!-- * Belongs to the main form. -->

4. **Hidden campaign source**

    * A hidden field named `sourceCampaign` with the value `summer2026_wellness`.

    <!-- * Belongs to the main form. -->

* * *

## **Section 5: Workshop Preferences & Additional Notes**

* Visually grouped and labeled “Section 5: Workshop Preferences & Additional Notes”. Belongs to the main form.

1. **Workshop Topics of Interest**

    * A group of toggleable options (checkboxes) that share the name `workshopTopics`.

    * “Nutrition & Diet” (value `nutrition`, pre‑checked).

    * “Exercise Physiology” (value `exercise`, unchecked).

    * “Mental Health & Mindfulness” (value `mental_health`, pre‑checked).

    * “Sleep Science” (value `sleep_science`, unchecked).

    * “Genetics & Epigenetics” (value `genetics`, disabled and unchecked, with a note that it is currently unavailable).

    * All belong to the main form.

2. **Additional Notes**

    * A multi‑line text area.

    * Visible size: 8 rows tall and 60 columns wide.

    * Placeholder: “Please provide any additional information relevant to your participation in this research study...”

    * Maximum length: 2000 characters; minimum: 10 characters.

    <!-- * Autocomplete is disabled. -->

    * The text should visually wrap (soft wrapping) without modifying the submitted data.

    * The field must submit the directionality of the text under a name ending in `.dir`.

    <!-- * On‑screen keyboard: standard text. -->

    <!-- * Belongs to the main form. -->

* * *

## **Section 6: Review & Submit**

* Visually grouped and labeled “Section 6: Review & Submit”. Belongs to the main form.

1. **Primary Submit button**

    * A clickable button that submits the form.

    * It must send the data to `https://example.com/api/submit-research-registration` using POST and multipart encoding, and open the result in a new tab.

    * The button’s name is `submitAction` and its value is `final_submit`.

    * The visible label includes the text “✅ Submit Registration”.

    <!-- * Belongs to the main form. -->

2. **Save as Draft button**

    * A submit button that behaves differently: it must send the form data to `https://example.com/api/save-draft` using POST, but with URL‑encoded format (not multipart).

    * Client‑side validation must be skipped entirely for this button.

    * The result must open in the same window.

    * Button name: `submitAction`, value: `save_draft`.

    * Label: “💾 Save as Draft (no validation)”.

    <!-- * Belongs to the main form. -->

3. **Reset button (button element)**

    * A button that resets all form fields to their initial values.

    * Name: `resetAction`, value: `reset_form`.

    * Label: “🔄 Reset All Fields”.

    <!-- * Belongs to the main form. -->

4. **Toggle Help button**

    * A push button that does **not** submit the form.

    * When activated, it toggles the visibility of a help information panel (described later).

    * Name: `helpButton`, value: `show_help`.

    * Label: “❓ Toggle Help”.

    <!-- * Belongs to the main form. -->

5. **Graphical Submit via Image**

    * An image that acts as a submit button.

    * The image must be a green rectangle 180×44 pixels with the white text “📋 Submit with Image” centered on it. It must be embedded directly as a data URI (for example: `data:image/svg+xml,...`).

    * When clicked, the form must submit to `https://example.com/api/image-submit` using POST and multipart encoding, skipping validation, and open the result in a new tab.

    * The click coordinates (x,y) must be appended to the form data.

    * Name: `imageSubmit`, value: `graphical_submit`.

    <!-- * Belongs to the main form. -->

6. **Plain button input**

    * A button with no default behavior, just a clickable element.

    * Label: “This is an input\[type=button\]”.

    * Name: `plainInputButton`.

    <!-- * Belongs to the main form. -->

* * *

**Help Popover (outside the form structure)**

* A panel with the identifier `helpPopover` that appears as a popover when triggered.

* It contains a heading “Help & Instructions” and two paragraphs with instructions about completing the fields, saving drafts, and privacy.

* The button with label “❓ Toggle Help” (from Section 6) must toggle this popover.

* * *

**External Section (physically after the closing tag of the main form, but still part of it)**

* All the following elements must be associated with the main form (`researchRegistration`) so that they are submitted with it, even though they are outside the `<form>` container.

* The group must be visually labelled “External Section (Associated via form attribute)”.

1. **Referral Source text field**

    * Label: “Referral Source (external field):”.

    * Text input, placeholder “How did you hear about us?”, visible width 35 characters.

    * Belongs to the main form (externally linked).

2. **Preferred Study Site dropdown**

    * Label: “Preferred Study Site:”.

    * Single‑selection list. First option “-- Choose a site --” disabled and pre‑selected.

    * Options: “Site A - Downtown Medical Center” (value `site_a`), “Site B - North Campus Research Park” (`site_b`), “Site C - South Community Clinic” (`site_c`).

    * Belongs to the main form.

3. **External Notes textarea**

    * Label: “External Notes:”.

    * 3 rows, 45 columns. Placeholder “Any additional comments...”.

    * Belongs to the main form.

4. **Study Enrollment Score output**

    * Label: “Study Enrollment Score (external output):”.

    * A read‑only display linked to the weight, height, and pain level fields for calculation.

    * Shows “N/A” initially. Name `enrollmentScore`.

    * Belongs to the main form.

5. **Overall Study Readiness gauge**

    * A gauge (meter) showing 65% readiness, ranging from 0 to 100.

    * Low threshold 40, high threshold 80, optimum 90.

    * Displays “65%” as text.

    * Belongs to the main form.

6. **External Submit button**

    * A submit button that sends the form data to `https://example.com/api/external-submit` using POST, multipart encoding, opening result in a new tab.

    * Name `externalSubmit`, value `external_submit_button`.

    * Label “📤 Submit from External Section”.

    * Belongs to the main form.

7. **External Consent checkbox**

    * A checkbox with label “I consent to the external data processing terms.”

    * Value `agreed`, name `externalConsent`.

    <!-- * The label itself is also explicitly linked to the main form. -->

    * Belongs to the main form.

* * *

These requirements describe every interactive and non‑interactive form element exactly as they must appear and behave, leaving no room for ambiguity in implementation.
